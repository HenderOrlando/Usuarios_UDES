CREATE TABLE usuario (id INT UNSIGNED AUTO_INCREMENT, code INT UNSIGNED NOT NULL, name VARCHAR(30) NOT NULL, password VARCHAR(25) NOT NULL, email VARCHAR(40) NOT NULL, date DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
